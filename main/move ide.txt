float tx=100, ty=100, px=100, py=100;
 
void setup() {
  size(400, 400);
}
 
void draw() {
  px = lerp(px, tx, 0.01);
  py = lerp(py, ty, 0.01);
  background(0);
  ellipse(px, py, 10, 10);
}
 
void mousePressed() {
  tx = mouseX;
  ty = mouseY;
}